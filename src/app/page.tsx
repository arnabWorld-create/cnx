export default function Page() {
  // Temporary: serve your existing static HTML from `public/index.html`.
  // Later we can convert it to real React components.
  return (
    <iframe
      src="/cnx"
      title="CNX Healthcare"
      style={{ width: "100%", height: "100vh", border: 0 }}
    />
  );
}
